1. Put all files in a directory
2. Edit CMakeLists.txt file to include exe and additional files
3. From the directory with the source and cmakelists.txt files 
4.Opens a terminal window (CLI) 
	mkdir build
	cd build
	cmake -G "Visual Studio 16 2019" ..
	cmake --build . --config Release
5. Test the program in the Release subfolder.
6. Make a zip file and submit
